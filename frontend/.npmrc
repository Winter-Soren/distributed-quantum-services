# Use hoisted linker so overrides can patch nested deps like next's postcss
node-linker=hoisted
# Prevent accidental use of wrong package manager  
engine-strict=true
# Don't prompt in non-TTY environments
confirm-module-purge=false
